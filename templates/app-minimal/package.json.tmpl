{
  "name": "{{name}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "boot": "node --experimental-strip-types src/app.ts"
  },
  "dependencies": {
    "@arki/dot": "^{{dotVersion}}",
    "@arki/env": "^{{envVersion}}",
    "zod": "^{{zodVersion}}"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
